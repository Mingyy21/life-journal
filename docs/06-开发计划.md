# 开发计划

## 总体时间线

| Phase | 内容 | 耗时 | 状态 |
|-------|------|------|------|
| Phase 0 | 项目脚手架 + 配置 | 1-2天 | ✅ 完成 |
| Phase 1 | 日记CRUD + 基础UI | 3-5天 | 源码完成，待编译验证 |
| Phase 2 | AI分析集成 | 2-3天 | 源码完成，待API联调 |
| Phase 3 | PWA + 移动端优化 | 2-3天 | 待开发 |
| Phase 4 | 可视化 + 测试 | 2-3天 | 待开发 |
| Phase 5 | 部署上线 | 1天 | 待部署 |

**MVP预计总开发周期**：2-3周（业余时间）

---

## Phase 0：项目脚手架（✅ 完成）

- [x] 项目目录结构创建
- [x] package.json（依赖清单）
- [x] tsconfig.json（strict模式，路径别名@/）
- [x] next.config.js
- [x] tailwind.config.ts（自定义主题：primary/warm/calm色板 + 字体）
- [x] postcss.config.js
- [x] .env.example / .env.local
- [x] 设计文档（12份）

---

## Phase 1：日记CRUD + 基础UI（源码完成，待验证）

### 已完成文件
- [x] `src/types/index.ts` — 所有TypeScript类型定义
- [x] `src/lib/db.ts` — Dexie.js数据库：Schema + CRUD + 预设8个课题
- [x] `src/hooks/useDatabase.ts` — 数据库初始化Hook
- [x] `src/hooks/useDiary.ts` — 日记CRUD操作Hook
- [x] `src/components/Layout.tsx` — 全局布局（Header + 底部导航）
- [x] `src/components/DiaryEditor.tsx` — 日记编辑器（标题+内容+课题+标签）
- [x] `src/components/DiaryList.tsx` — 日记列表（日期分组+预览+标签色块）
- [x] `src/components/DiaryDetail.tsx` — 日记详情卡片
- [x] `src/components/TagFilter.tsx` — 标签筛选器（课题→标签联动）
- [x] `src/app/layout.tsx` — 根布局 + PWA Metadata
- [x] `src/app/page.tsx` — 首页（集成编辑器+筛选+列表）
- [x] `src/app/globals.css` — 全局样式（配色+字体+动画+滚动条）

### 待验证
- [ ] `npm install` 安装依赖无报错
- [ ] `npm run dev` 启动成功
- [ ] 浏览器打开页面正常渲染
- [ ] IndexedDB初始化成功（DevTools > Application > IndexedDB > LifeJournalDB）
- [ ] 创建日记→保存→出现在列表
- [ ] 点击标签→筛选正确
- [ ] 点击日记→进入详情页

---

## Phase 2：AI分析集成（源码完成，待联调）

### 已完成文件
- [x] `src/lib/ai.ts` — DeepSeek API调用 + System Prompt + 模拟分析降级
- [x] `src/components/AnalysisCard.tsx` — 分析卡片（情绪条+VAD+认知阶段+洞察+反馈）
- [x] `src/app/api/analyze/route.ts` — API Route（Edge Runtime）
- [x] `src/app/diary/[id]/page.tsx` — 日记详情页（集成分析卡片）

### 待验证
- [ ] 模拟分析模式：不配API Key，点分析→出模拟结果→存入缓存
- [ ] 真实API模式：配API Key，点分析→调用DeepSeek→展示结果→存入缓存
- [ ] 缓存验证：分析过的日记再次打开，直接读缓存，不重复请求
- [ ] 错误处理：API超时→降级为模拟分析→显示提示信息
- [ ] 内容太短提示（<10字）→返回400错误

---

## Phase 3：PWA + 移动端优化

### 待完成
- [ ] Service Worker配置（Serwist）
- [ ] manifest.json（应用名称/图标/主题色/显示模式）
- [ ] 离线缓存策略（静态资源CacheFirst + 页面NetworkFirst）
- [ ] 移动端UI适配测试（iOS Safari + Android Chrome）
- [ ] "添加到主屏幕"功能测试
- [ ] 断网测试（关闭网络后能正常打开+写日记+查看历史）
- [ ] 图标生成（favicon + apple-touch-icon + PWA icons）

---

## Phase 4：可视化 + 测试

### 可视化（ECharts集成）
- [ ] 情绪趋势折线图（最近30天愉悦度/唤醒度变化）
- [ ] 课题分布饼图（各课题记录数量占比）
- [ ] 情绪日历热力图（哪天写了日记，什么情绪颜色）
- [ ] 课题进度雷达图（8个课题的消化进度，v0.2正式使用）

### 测试

#### 单元测试（Vitest）
- [ ] 数据库CRUD操作（db.ts）
- [ ] AI分析函数（Mock模式）
- [ ] 模拟分析逻辑（给定文本→预期情绪输出）
- [ ] 标签筛选逻辑

#### E2E测试（Playwright）
- [ ] 完整用户流程：打开App → 写日记 → 选标签 → 保存 → 筛选 → 查看 → AI分析
- [ ] 移动端视口：iPhone SE(375×812)、iPhone 14 Pro(390×844)
- [ ] 离线模式：断网 → 页面正常加载 → 写日记成功 → 联网后数据仍在
- [ ] 多标签筛选：选课题 → 展开细化标签 → 选细化标签 → 正确筛选

---

## Phase 5：部署上线

### Vercel部署
- [ ] Vercel项目创建 + GitHub连接
- [ ] 环境变量配置（DEEPSEEK_API_KEY等）
- [ ] 自动部署（push main → 自动构建部署）
- [ ] 自定义域名配置（可选）

### 预发布验证清单
- [ ] HTTPS正常工作
- [ ] PWA可安装（Chrome + Safari + 移动端）
- [ ] API Route正常工作（Edge Runtime）
- [ ] IndexedDB在HTTPS下正常（无隐私模式限制）
- [ ] 移动端浏览器完整走通全流程
- [ ] 性能检查（Lighthouse > 90）

---

## 下一步行动（当前开发者）

```bash
# 在VSCode终端中执行
cd "D:\总文件夹\项目\人生手记"
npm install
npm run dev
# 打开 http://localhost:3000
# 修复所有编译错误
```
