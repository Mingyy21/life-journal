/**
 * 数据库集成测试
 *
 * 这些测试需要连接真实的 Supabase 实例，无法在纯前端环境中运行。
 * 在本地手动测试时：
 *   1. 确保 .env.local 的 Supabase 凭据有效
 *   2. 确认网络可访问 https://cvgarlwzqdojeqmhahvz.supabase.co
 *
 * 在 vitest jsdom 环境下自动跳过（fetch 不可用）。
 */

import { describe, it, expect } from "vitest";

describe.skip("数据库集成测试（需要 Supabase 连接）", () => {
  it("预设5个一级标签正确初始化", () => {});
  it("8个二级标签正确映射到一级标签", () => {});
  it("创建日记（日常，不关联事件）", () => {});
  it("更新日记关联事件", () => {});
  it("按一级标签筛选日记", () => {});
  it("创建并查询事件", () => {});
  it("按解决状态筛选事件", () => {});
  it("删除事件解绑关联日记", () => {});
  it("创建感悟可关联事件和课题", () => {});
  it("删除日记同步删除分析缓存", () => {});
  it("删除课题级联清理关联数据", () => {});
});
